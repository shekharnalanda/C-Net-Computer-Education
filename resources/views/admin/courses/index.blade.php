@extends('admin.layout')@section('title','Course Management')@section('content')<div class="panel"><h2>Add Course</h2><form class="form-grid" method="post" action="{{ route('admin.courses.store') }}">@csrf<label>Code<input name="code" required></label><label>Duration<input name="duration" required></label><label class="full">English Title<input name="title" required></label><label class="full">Hindi Title<input name="title_hi"></label><label>Category<select name="level" required>@foreach(['Foundation','Job-ready','Career','Technical','Creative','Future Skill'] as $x)<option>{{ $x }}</option>@endforeach</select></label><label>Eligibility<input name="eligibility"></label><label class="full">Summary<textarea name="summary" required></textarea></label><label class="full">Modules — one per line<textarea name="modules_text"></textarea></label><label class="full">Career opportunities — one per line<textarea name="careers_text"></textarea></label><label>Sort order<input type="number" name="sort_order" value="0"></label><label><input type="checkbox" name="is_active" value="1" checked style="width:auto"> Active</label><button class="btn full">Add Course</button></form></div><div class="panel"><h2>All Courses</h2>@foreach($courses as $course)<details style="border-bottom:1px solid #ddd;padding:14px 0"><summary><b>{{ $course->code }} — {{ $course->title }}</b> · {{ $course->duration }} · {{ $course->is_active?'Active':'Hidden' }}</summary><form class="form-grid" method="post" action="{{ route('admin.courses.update',$course) }}" style="margin-top:16px">@csrf @method('PUT')<label>Code<input name="code" value="{{ $course->code }}" required></label><label>Duration<input name="duration" value="{{ $course->duration }}" required></label><label class="full">English Title<input name="title" value="{{ $course->title }}" required></label><label class="full">Hindi Title<input name="title_hi" value="{{ $course->title_hi }}"></label><label>Category<input name="level" value="{{ $course->level }}" required></label><label>Eligibility<input name="eligibility" value="{{ $course->eligibility }}"></label><label class="full">Summary<textarea name="summary">{{ $course->summary }}</textarea></label><label class="full">Modules<textarea name="modules_text">{{ implode("\n",$course->modules ?: []) }}</textarea></label><label class="full">Careers<textarea name="careers_text">{{ implode("\n",$course->careers ?: []) }}</textarea></label><label>Sort order<input type="number" name="sort_order" value="{{ $course->sort_order }}"></label><label><input type="checkbox" name="is_active" value="1" @checked($course->is_active) style="width:auto"> Active</label><button class="btn full">Save Changes</button></form><form method="post" action="{{ route('admin.courses.destroy',$course) }}" onsubmit="return confirm('Delete this course?')" style="margin-top:10px">@csrf @method('DELETE')<button class="btn btn-danger">Delete Course</button></form></details>@endforeach</div>@endsection
